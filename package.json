{
  "name": "formulario-fic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "clear && vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "json": "clear && json-server --watch data.json --port 3000"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "zod": "^3.25.56"
  }
}